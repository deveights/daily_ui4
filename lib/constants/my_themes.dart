import 'package:flutter/material.dart';
import 'package:intl/intl.dart';

const primaryColor = Color.fromARGB(255, 255, 102, 102);
final formatter = NumberFormat.compact(locale: 'en_US', explicitSign: false);
